import { addKeyword } from '@builderbot/bot'

export default addKeyword(['continuar'])
    .addAnswer(
        [ 
            'Proceso en construcción...',
            'Esperamos pronto seguir atentiendo tus requerimientos'
        ]
    )


